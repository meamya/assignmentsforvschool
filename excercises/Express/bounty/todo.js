//create an array of new todo items to be posted to the arrayvar express = require("express");,


//generate a unique id for that todo (consider using the uuid npm package),


//return the entire list of todos,
var express = require("express");

//allow the user to "update" a todo by its _id,
app.put
//allow the user to delete a todo by its _id, and

app.delete
//allows the user retrieve a single todo by its _id.

app.get