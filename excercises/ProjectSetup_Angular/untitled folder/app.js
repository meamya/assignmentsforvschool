var app = angular.module("MyApp", []);
app.controller("controller", ["$scope", function ($scope) {
    $scope.myArray = [
                    "couch", " table", "lamp"]
            }]);

function (str) {
    for str.length