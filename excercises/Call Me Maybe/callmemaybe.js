var names = ["Anna", "Bill", "Charlay", "Dexter", "Emily", "Frank", "George", "Hal", "Isaac", "Jill"];  


//function called attemptcall use a forloop. If name has an odd length of characters, invoke don't call. If the name has an even number, invoke the call. If the name has one letter a, invoke send text. send text is higher than odd even.

function attemptcall
     

//send message that says "you did not call "" or calling "name" use setTimeOut. 
